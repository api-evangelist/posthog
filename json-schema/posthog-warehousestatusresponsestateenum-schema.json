{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/WarehouseStatusResponseStateEnum",
  "title": "WarehouseStatusResponseStateEnum",
  "enum": [
    "pending",
    "provisioning",
    "ready",
    "failed",
    "deleting",
    "deleted"
  ],
  "type": "string",
  "description": "* `pending` - pending\n* `provisioning` - provisioning\n* `ready` - ready\n* `failed` - failed\n* `deleting` - deleting\n* `deleted` - deleted"
}