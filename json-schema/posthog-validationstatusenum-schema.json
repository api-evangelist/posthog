{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ValidationStatusEnum",
  "title": "ValidationStatusEnum",
  "enum": [
    "valid",
    "invalid",
    "expired",
    "stale"
  ],
  "type": "string",
  "description": "* `valid` - Valid\n* `invalid` - Invalid\n* `expired` - Expired\n* `stale` - Stale (resource changed)"
}