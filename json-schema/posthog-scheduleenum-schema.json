{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ScheduleEnum",
  "title": "ScheduleEnum",
  "enum": [
    "once",
    "recurring",
    "always"
  ],
  "type": "string",
  "description": "* `once` - once\n* `recurring` - recurring\n* `always` - always"
}