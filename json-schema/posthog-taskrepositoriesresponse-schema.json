{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/TaskRepositoriesResponse",
  "title": "TaskRepositoriesResponse",
  "type": "object",
  "properties": {
    "repositories": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Distinct repositories in use by non-deleted, non-internal tasks for the current team."
    }
  },
  "required": [
    "repositories"
  ]
}