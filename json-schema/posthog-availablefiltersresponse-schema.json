{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/AvailableFiltersResponse",
  "title": "AvailableFiltersResponse",
  "type": "object",
  "properties": {
    "static_filters": {
      "allOf": [
        {
          "$ref": "#/components/schemas/StaticFilters"
        }
      ],
      "description": "Pre-computed filter options for scopes, activities, and users."
    },
    "detail_fields": {
      "type": "object",
      "additionalProperties": true,
      "description": "Discovered detail fields and their value distributions."
    }
  },
  "required": [
    "detail_fields",
    "static_filters"
  ]
}